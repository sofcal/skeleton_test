'use strict';

/** validate any additional config properties required by this lambda. Values required by the base class are validated there */
module.exports = (/* config, { logger } */) => {
    // no additional config required for this lambda, so nothing to validate.
};
