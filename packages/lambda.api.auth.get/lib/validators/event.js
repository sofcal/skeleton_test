'use strict';

// const access = require('safe-access');
// const _ = require('underscore');

module.exports = (/* event */) => {
    // const { authorization_id, bank, client_id, redirect_uri, response_type, state } = event.queryStringParameters;

    // _.extend(event.validated, {  });
};
